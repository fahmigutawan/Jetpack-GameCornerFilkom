package com.example.sigacorfilkom

class SnackbarHandler {
    companion object{
        fun showSnackbar (
            message: String
        ) {
            _showSnackbar(message)
        }
    }
}